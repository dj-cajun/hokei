/** 자동 생성 — scripts/prisma-generate-for-deploy.ts (직접 수정하지 마세요) */
export const PRISMA_DATASOURCE_PROVIDER = "postgresql" as const;

export type PrismaDatasourceProvider = typeof PRISMA_DATASOURCE_PROVIDER;
