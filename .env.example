# 로컬 개발 — Neon dev 브랜치 Connect URL (자동 삭제 끄기 권장)
DATABASE_URL="postgresql://USER:PASSWORD@ep-....neon.tech/neondb?sslmode=require"
AUTH_SECRET="your-secret-key-change-in-production"

# 소셜 로그인 (Google GIS)
NEXT_PUBLIC_SITE_URL="http://localhost:3001"
NEXT_PUBLIC_GOOGLE_CLIENT_ID=""
# 서버 토큰 검증용 (보통 NEXT_PUBLIC_GOOGLE_CLIENT_ID와 동일)
# GOOGLE_CLIENT_ID=""
# One Tap 기본 OFF(redirect 버튼만). FedCM AbortError 방지
# NEXT_PUBLIC_GOOGLE_ONE_TAP="true"
# NEXT_PUBLIC_GOOGLE_DISABLE_ONE_TAP="true"

# Z.AI — 비한국어 기사 번역용 (선택, .env에 키 있으면 사용)
ZAI_API_KEY="your-z-ai-api-key"
ZAI_MODEL="glm-4-flash"

# 네이버 검색 API — Vercel에도 설정 시 REST 검색 + RSS 혼합 (Playwright는 Vercel 불가)
# https://developers.naver.com/apps
# Secret은 보통 20자 이상. 024 오류 시 검색 API 활성화·전체 Secret 재입력
NAVER_CLIENT_ID=""
NAVER_CLIENT_SECRET=""

# 강제 RSS-only (네이버 키 있어도 VnExpress RSS만)
# INGEST_RSS_ONLY=1

# Jina Reader — 차단·JS 페이지 본문 폴백 (관리자 URL 가져오기·수집)
# https://jina.ai/reader — 키 없이도 동작, JINA_API_KEY 있으면 한도 완화
# JINA_API_KEY=""
# JINA_READER_ENABLED=0

# Gemini (비한국어·VnExpress RSS 번역, 선택)
# GEMINI_API_KEY=""
# GEMINI_MODEL="gemini-2.5-flash-lite"

# OpenRouter — AI 카톡 큐레이션 (/admin/ai-curate)
# https://openrouter.ai/keys
# OPENROUTER_API_KEY=""
# OPENROUTER_MODEL="openai/gpt-4o-mini"

# Google Cloud Translation API (선택)
# GOOGLE_TRANSLATE_API_KEY=""

# Vercel Cron / 수동 수집 API 보호 (프로덕션 필수)
CRON_SECRET="your-random-cron-secret"
# 뉴스 자동 수집: 매일 07:00·12:00 ICT (vercel.json cron 0 0·0 5 * * * UTC)
# 일일 상한 — 미설정·0 = 무제한 (기본). 과거 15건 제한 복구: NEWS_DAILY_CAP=15
# NEWS_DAILY_CAP=0
# 1회 수집 상한 (무제한일 때, Vercel Cron 기본 15) — NEWS_PER_RUN_CAP=15

# 로컬 프로덕션 스모크 (next start) 시 Auth.js 호스트 신뢰
# AUTH_TRUST_HOST=true

# 검색 — 최초 1회: npm run search:pg:setup

# 프로덕션 Neon (로컬 CLI 전용, npm run news:prod:*)
# .env.production.pg 에 production 브랜치 URL — docs/DATABASE.md

# Docker PostgreSQL (오프라인 로컬, 선택)
# npm run setup:pg
# DATABASE_URL="postgresql://hokei:hokei_local@localhost:5432/hokei"

# Sentry — https://sentry.io (org: nam-bac-technology-and-service / project: javascript-nextjs)
# Settings → Client Keys (DSN) 에서 복사 · docs/SENTRY.md
# SENTRY_DSN=""
# NEXT_PUBLIC_SENTRY_DSN=""
# SENTRY_ORG="nam-bac-technology-and-service"
# SENTRY_PROJECT="javascript-nextjs"

# Upstash Redis — Vercel 다중 인스턴스 Rate limit (선택, 없으면 메모리 제한)
# https://console.upstash.com/
# UPSTASH_REDIS_REST_URL=""
# UPSTASH_REDIS_REST_TOKEN=""

# Vercel Blob — 프로덕션 첨부 업로드 필수 (Storage → Blob)
# Vercel Storage → Blob (글 첨부 + 자동 뉴스 썸네일 복사)
# BLOB_READ_WRITE_TOKEN=""

# 호치민 날씨 (WeatherAPI.com — https://www.weatherapi.com/signup.aspx)
# WEATHERAPI_KEY=""

# 회원가입 이메일 인증 (Resend — https://resend.com)
# RESEND_API_KEY=""
# EMAIL_FROM="Hokei <hello@hokei.vn>"
# CONTACT_EMAIL="webmaster@hokei.vn"
# AD_CONTACT_EMAIL="ads@hokei.vn"
# CONTACT_INBOX_EMAIL="your@gmail.com"  # 문의 폼 실제 수신 (미설정 시 ads@/webmaster@)
# CONTACT_ADMIN_USER_ID=""  # 쪽지 수신 관리자 user id (미설정 시 role=ADMIN 전원)

# GOOGLE_SITE_VERIFICATION=""  # Search Console HTML 태그 content 값만 (메타 인증)
# NEXT_PUBLIC_ADSENSE_CLIENT="ca-pub-xxxxxxxxxxxxxxxx"
# NEXT_PUBLIC_ADSENSE_SLOT_ARTICLE="1234567890"
# NEXT_PUBLIC_ADSENSE_SLOT_HOME="0987654321"

# 홈 하이라이트 YouTube (미설정 시 d-fY16xMeT4, 12초부터)
# NEXT_PUBLIC_YOUTUBE_HIGHLIGHT_ID="d-fY16xMeT4"
# NEXT_PUBLIC_YOUTUBE_HIGHLIGHT_START="12"

# 프로덕션 배포: npm run deploy:full (git push → Vercel 자동, CLI 불필요)
# VERCEL_TOKEN 은 .env에 넣지 마세요 — 만료 시 CLI가 막힙니다. 필요 시 npx vercel login
